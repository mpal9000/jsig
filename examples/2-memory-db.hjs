-- vim: set ft=Haskell:
interface TDB {
    _values: Object<String, String>,

    get(key: String) => String,
    set(key: String, value: String) => void,
    keys() => Array<String>
}

new DB() => TDB
DB : DB
